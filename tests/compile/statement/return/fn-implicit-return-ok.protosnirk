// Functions may be implicitly returned from
// Block implicit return is checked later

fn stmtRetVoid()
    let x = 0
    // implicitly return void

fn exprRetVoid(x: float)
    x + 1

fn exprRetExpr() -> float
    let y = 1
    y + 2
